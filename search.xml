<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>CentOS 설치하기</title>
    <url>/2019/08/01/2019-08-02-install-centos/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2>

<p>운영체제 CentOS를 설치하는 방법을 정리한다.</p>
<span id="more"></span>

<h3 id="버전"><a href="#버전" class="headerlink" title="버전"></a>버전</h3><p>2019년 07월 29일 기준으로 최신 버전인 CentOS 7 1810 버전을 이용하여 설명한다.</p>
<h2 id="CentOS-다운로드"><a href="#CentOS-다운로드" class="headerlink" title="CentOS 다운로드"></a>CentOS 다운로드</h2><p><a href="https://www.centos.org/download/">https://www.centos.org/download/</a></p>
<p>위 경로에서 최신 버전의 CentOS DVD ISO 파일을 다운로드한다.</p>
<h2 id="CentOS-설치-환경-구성"><a href="#CentOS-설치-환경-구성" class="headerlink" title="CentOS 설치 환경 구성"></a>CentOS 설치 환경 구성</h2><p>다음 중 하나의 방식으로 CentOS 설치 환경을 구성한다.</p>
<ul>
<li>로컬 컴퓨터에 VMware 등의 가상화 도구를 이용하여 가상 머신 생성</li>
<li>직접 CentOS를 설치할 컴퓨터 준비<ul>
<li>Rufus 등의 도구를 이용하여 CentOS 설치 USB 생성</li>
</ul>
</li>
</ul>
<h2 id="CentOS-설치"><a href="#CentOS-설치" class="headerlink" title="CentOS 설치"></a>CentOS 설치</h2><p>설치 이미지를 실행하여 가이드에 따라 한국어를 선택하여 설치를 진행한다.</p>
<p>다음과 같이 설정하고 설치 시작 버튼을 클릭한다.</p>
<ul>
<li>설치 대상<ul>
<li>자동 파티션 설정 선택</li>
</ul>
</li>
<li>네트워크 및 호스트명<ul>
<li>유선으로 … 연결되었습니다</li>
</ul>
</li>
<li>그 외 기본값</li>
</ul>
<p>설치되는 동안 루트 계정의 암호를 설정하고 관리자 계정을 생성한다.</p>
<p>설치가 완료될 때까지 대기한 후 안내에 따라 기기를 재시작한다.</p>
<p>그러면 CentOS 설치가 완료된다.</p>
<h2 id="추가로-확인할-사항"><a href="#추가로-확인할-사항" class="headerlink" title="추가로 확인할 사항"></a>추가로 확인할 사항</h2><h3 id="방화벽으로-인한-문제"><a href="#방화벽으로-인한-문제" class="headerlink" title="방화벽으로 인한 문제"></a>방화벽으로 인한 문제</h3><p>CentOS를 설치하면 기본적으로 방화벽 소프트웨어가 실행되고 있다.</p>
<p>이로 인하여 사용자 설정으로 개방한 포트에 대하여 외부 접근이 불가능할 수 있다.</p>
<p>이 때는 다음과 같이 방화벽에 원하는 서비스나 포트를 등록하여 문제를 해결할 수 있다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo firewall-cmd --permanent --add-service=http</span><br><span class="line">sudo firewall-cmd --permanent --add-port=8080/tcp</span><br><span class="line">sudo firewall-cmd --reload</span><br><span class="line">sudo firewall-cmd --permanent --list-all</span><br></pre></td></tr></table></figure>

<h3 id="기본-소프트웨어-설치"><a href="#기본-소프트웨어-설치" class="headerlink" title="기본 소프트웨어 설치"></a>기본 소프트웨어 설치</h3><p>CentOS를 최소 설치를 선택하여 설치하면 설치 속도는 빨라지지만 시스템 관리 및 서비스 구동에 필요한 기본적인 소프트웨어도 설치되어 있지 않은 상태가 된다.</p>
<p>다음 목록을 참고하여 기본 소프트웨어 설치를 위한 패키지를 설치할 수 있다.</p>
<ul>
<li>ifconfig<ul>
<li>net-tools</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>시스템 구축</category>
      </categories>
      <tags>
        <tag>CentOS</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS에 Docker 설치하기</title>
    <url>/2019/08/03/2019-08-03-install-docker-on-centos/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2>

<p>CentOS에 Docker를 설치하는 방법을 정리한다.</p>
<span id="more"></span>

<h2 id="설치"><a href="#설치" class="headerlink" title="설치"></a>설치</h2><h3 id="Update-packages"><a href="#Update-packages" class="headerlink" title="Update packages"></a>Update packages</h3><p>다음 명령어로 yum으로 설치된 모든 패키지의 업데이트를 진행한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo yum update -y</span><br></pre></td></tr></table></figure>

<h3 id="Docker-설치"><a href="#Docker-설치" class="headerlink" title="Docker 설치"></a>Docker 설치</h3><p>다음 명령어로 yum을 이용하여 Docker를 설치한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo yum install -y docker</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 Docker 서비스를 시작한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo systemctl start docker</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 부팅 시 Docker 서비스를 자동으로 시작하도록 설정한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure>

<h2 id="추가로-확인할-사항"><a href="#추가로-확인할-사항" class="headerlink" title="추가로 확인할 사항"></a>추가로 확인할 사항</h2><h3 id="Insecure-Registry-등록"><a href="#Insecure-Registry-등록" class="headerlink" title="Insecure Registry 등록"></a>Insecure Registry 등록</h3><p>다음과 같이 Docker daemon 설정 파일을 수정하여 (없으면 생성) 다음과 같은 구문을 추가한다.</p>
<figure class="highlight json"><figcaption><span>/etc/docker/daemon.json</span></figcaption><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;insecure-registries&quot;</span>: [<span class="string">&quot;URL:PORT&quot;</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>그 후 Docker 서비스를 재시작하면 해당 URL에 대하여 Insecure registry 오류가 발생하지 않는다.</p>
<p>하지만 이 설정은 서버와 통신할 때 적절한 인증서 검증을 사용하지 않게 만들기 때문에 주의가 필요하다.</p>
]]></content>
      <categories>
        <category>시스템 구축</category>
      </categories>
      <tags>
        <tag>CentOS</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS에 JDK 설치하기</title>
    <url>/2019/08/04/2019-08-04-install-jdk-on-centos/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2>

<p>CentOS 환경에 Java를 설치하는 방법을 정리한다.</p>
<span id="more"></span>

<h2 id="OpenJDK"><a href="#OpenJDK" class="headerlink" title="OpenJDK"></a>OpenJDK</h2><h3 id="OpenJDK-버전"><a href="#OpenJDK-버전" class="headerlink" title="OpenJDK 버전"></a>OpenJDK 버전</h3><p>2019년 07월 29일 기준으로 최신 버전인 build 12+32를 이용하여 설명한다.</p>
<h3 id="OpenJDK-다운로드"><a href="#OpenJDK-다운로드" class="headerlink" title="OpenJDK 다운로드"></a>OpenJDK 다운로드</h3><p><a href="http://jdk.java.net/">http://jdk.java.net</a></p>
<p>위 사이트에서 JDK 최신 버전의 링크를 복사한다.</p>
<p>본 문서 작성일 기준으로 다음과 같았다.</p>
<p><a href="https://download.java.net/openjdk/jdk12/ri/openjdk-12+32_linux-x64_bin.tar.gz">https://download.java.net/openjdk/jdk12/ri/openjdk-12+32_linux-x64_bin.tar.gz</a></p>
<p>다음 명령어로 해당 JDK 압축 파일을 다운로드한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://download.java.net/openjdk/jdk12/ri/openjdk-12+32_linux-x64_bin.tar.gz</span><br></pre></td></tr></table></figure>

<h3 id="OpenJDK-설치"><a href="#OpenJDK-설치" class="headerlink" title="OpenJDK 설치"></a>OpenJDK 설치</h3><p>다음 명령어로 다운로드 한 압축 파일의 압축을 해제한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -xzvf openjdk-12+32_linux-x64_bin.tar.gz</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 JDK 디렉토리를 이동하고 심볼릭 링크를 생성한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mv jdk-12 /usr/<span class="built_in">local</span>/</span><br><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span></span><br><span class="line">ln -sf jdk-12 java</span><br></pre></td></tr></table></figure>

<p>사용 중인 쉘의 rc 파일의 가장 아래에 다음 구문을 추가한다. (.bashrc, .zshrc 등)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=<span class="variable">$PATH</span>:/usr/<span class="built_in">local</span>/java/bin</span><br><span class="line"><span class="built_in">export</span> PATH</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 사용 중인 쉘의 rc 파일을 적용한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 Java와 컴파일러를 정상적으로 사용할 수 있는지 확인한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java --version</span><br><span class="line">javac --version</span><br></pre></td></tr></table></figure>

<h2 id="Oracle-JDK"><a href="#Oracle-JDK" class="headerlink" title="Oracle JDK"></a>Oracle JDK</h2><p>2018년 7월부터 Oracle JDK의 Java SE Subscription 라이센스 정책이 시행됨에 따라 상업용으로 이용하려면 라이센스 구독을 해야 한다.</p>
<p>다만 Oracle JDK 1.8.202 버전 이하는 여전히 프로그램의 동작을 위한 목적에 한하여 무료로 사용 가능한 Binary Code License(BCL) 정책을 따른다.</p>
<h3 id="Oracle-JDK-버전"><a href="#Oracle-JDK-버전" class="headerlink" title="Oracle JDK 버전"></a>Oracle JDK 버전</h3><p>개인용으로 사용할 것인지 상업용으로 사용할 것인지 등에 따라서 원하는 버전이 달라질 수 있다.</p>
<p>필자는 개인적으로 사용할 예정이므로 2019년 08월 04일 기준으로 최신 버전인 12.0.2를 기준으로 설명한다.</p>
<h3 id="Oracle-JDK-다운로드"><a href="#Oracle-JDK-다운로드" class="headerlink" title="Oracle JDK 다운로드"></a>Oracle JDK 다운로드</h3><p><a href="https://www.oracle.com/technetwork/java/javase/downloads/index.html">오라클 Java SE 다운로드 페이지</a>에 접속하여 원하는 버전을 다운로드한다.</p>
<p>오라클 웹 페이지에서는 접속하는 클라이언트가 쿠키 정책에 동의해야만 이후 작업을 처리해 주기 때문에 wget으로 간단하게 다운로드 하기 어렵다.</p>
<p>그래서 그냥 로컬 컴퓨터로 다운로드 하고 설치하려는 서버에 직접 옮기는 게 편하다.</p>
<h3 id="Oracle-JDK-설치"><a href="#Oracle-JDK-설치" class="headerlink" title="Oracle JDK 설치"></a>Oracle JDK 설치</h3><p>다음 명령어로 다운로드 한 압축 파일의 압축을 해제한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -xzvf jdk-12.0.2_linux-x64_bin.tar.gz</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 JDK 디렉토리를 이동하고 심볼릭 링크를 생성한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mv jdk-12.0.2 /usr/<span class="built_in">local</span>/</span><br><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span></span><br><span class="line">ln -sf jdk-12.0.2 java</span><br></pre></td></tr></table></figure>

<p>사용 중인 쉘의 rc 파일의 가장 아래에 다음 구문을 추가한다. (.bashrc, .zshrc 등)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=<span class="variable">$PATH</span>:/usr/<span class="built_in">local</span>/java/bin</span><br><span class="line"><span class="built_in">export</span> PATH</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 사용 중인 쉘의 rc 파일을 적용한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure>

<p>다음 명령어로 Java와 컴파일러를 정상적으로 사용할 수 있는지 확인한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java --version</span><br><span class="line">javac --version</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>시스템 구축</category>
      </categories>
      <tags>
        <tag>CentOS</tag>
        <tag>Java</tag>
        <tag>JDK</tag>
      </tags>
  </entry>
  <entry>
    <title>Node.js 설치하기</title>
    <url>/2019/08/06/2019-08-06-install-nodejs/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2>

<p><a href="https://github.com/nodesource/distributions/blob/master/README.md">https://github.com/nodesource/distributions/blob/master/README.md</a></p>
<p>위 페이지를 참고하여 Node.js를 설치하는 공식적인 방법을 정리한다.</p>
<span id="more"></span>

<h2 id="설치"><a href="#설치" class="headerlink" title="설치"></a>설치</h2><p>다음 명령어를 이용하여 각 환경에서 Node.js를 설치할 수 있다.</p>
<h3 id="Ubuntu-또는-Debian-기반"><a href="#Ubuntu-또는-Debian-기반" class="headerlink" title="Ubuntu 또는 Debian 기반"></a>Ubuntu 또는 Debian 기반</h3><h4 id="U-D-기반-Node-js-v12-x-설치"><a href="#U-D-기반-Node-js-v12-x-설치" class="headerlink" title="U/D 기반 Node.js v12.x 설치"></a>U/D 기반 Node.js v12.x 설치</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Using Ubuntu</span></span><br><span class="line">curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -</span><br><span class="line">sudo apt-get install -y nodejs</span><br><span class="line"></span><br><span class="line"><span class="comment"># Using Debian, as root</span></span><br><span class="line">curl -sL https://deb.nodesource.com/setup_12.x | bash -</span><br><span class="line">apt-get install -y nodejs</span><br></pre></td></tr></table></figure>

<h4 id="U-D-기반-Node-js-v11-x-설치"><a href="#U-D-기반-Node-js-v11-x-설치" class="headerlink" title="U/D 기반 Node.js v11.x 설치"></a>U/D 기반 Node.js v11.x 설치</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Using Ubuntu</span></span><br><span class="line">curl -sL https://deb.nodesource.com/setup_11.x | sudo -E bash -</span><br><span class="line">sudo apt-get install -y nodejs</span><br><span class="line"></span><br><span class="line"><span class="comment"># Using Debian, as root</span></span><br><span class="line">curl -sL https://deb.nodesource.com/setup_11.x | bash -</span><br><span class="line">apt-get install -y nodejs</span><br></pre></td></tr></table></figure>

<h4 id="U-D-기반-Node-js-v10-x-설치"><a href="#U-D-기반-Node-js-v10-x-설치" class="headerlink" title="U/D 기반 Node.js v10.x 설치"></a>U/D 기반 Node.js v10.x 설치</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Using Ubuntu</span></span><br><span class="line">curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -</span><br><span class="line">sudo apt-get install -y nodejs</span><br><span class="line"></span><br><span class="line"><span class="comment"># Using Debian, as root</span></span><br><span class="line">curl -sL https://deb.nodesource.com/setup_10.x | bash -</span><br><span class="line">apt-get install -y nodejs</span><br></pre></td></tr></table></figure>

<h3 id="Enterprise-Linux-기반-CentOS-RHEL-Fedora-등"><a href="#Enterprise-Linux-기반-CentOS-RHEL-Fedora-등" class="headerlink" title="Enterprise Linux 기반 (CentOS, RHEL, Fedora 등)"></a>Enterprise Linux 기반 (CentOS, RHEL, Fedora 등)</h3><h4 id="EL-기반-Node-js-v12-x-설치"><a href="#EL-기반-Node-js-v12-x-설치" class="headerlink" title="EL 기반 Node.js v12.x 설치"></a>EL 기반 Node.js v12.x 설치</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sL https://rpm.nodesource.com/setup_12.x | bash -</span><br></pre></td></tr></table></figure>

<h4 id="EL-기반-Node-js-v11-x-설치"><a href="#EL-기반-Node-js-v11-x-설치" class="headerlink" title="EL 기반 Node.js v11.x 설치"></a>EL 기반 Node.js v11.x 설치</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sL https://rpm.nodesource.com/setup_11.x | bash -</span><br></pre></td></tr></table></figure>

<h4 id="EL-기반-Node-js-v10-x-설치"><a href="#EL-기반-Node-js-v10-x-설치" class="headerlink" title="EL 기반 Node.js v10.x 설치"></a>EL 기반 Node.js v10.x 설치</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -sL https://rpm.nodesource.com/setup_10.x | bash -</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>시스템 구축</category>
      </categories>
      <tags>
        <tag>Node.js</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL 환경에 Ubuntu 설정하기</title>
    <url>/2019/08/08/2019-08-08-ubuntu-on-wsl/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2>

<p>Windows 10 버전 1607 이상에서는 가상 머신이 없이도 Linux 환경에서 작업할 수 있다.<br>바로 Windows Subsystem for Linux, 줄여서 WSL이 있기 때문이다.</p>
<p>이 글에서는 Windows Subsystem for Linux 환경을 이용하여 Windows를 사용하며 Ubuntu도 사용하는 방법을 정리한다.</p>
<span id="more"></span>

<h2 id="설치"><a href="#설치" class="headerlink" title="설치"></a>설치</h2><p>다음과 같은 순서로 WSL을 통해 Ubuntu를 설치할 수 있다.</p>
<ol>
<li>제어판 → 프로그램 → 프로그램 및 기능 → Windows 기능 켜기/끄기를 선택한다.</li>
<li>목록에서 Linux용 Windows 하위 시스템을 찾아 체크박스에 체크한다.</li>
<li>확인 버튼을 누르면 자동으로 설치가 되며 설치가 완료되면 안내에 따라 컴퓨터를 재부팅한다.</li>
<li>컴퓨터에 내장된 Microsoft Store를 열고 Ubuntu를 검색하여 설치한다.</li>
<li>설치가 완료되면 Ubuntu 앱을 연다.</li>
<li>콘솔 창이 뜨면 조금 기다린다.</li>
<li>Enter new UNIX username 문구가 나타나면 Ubuntu에서 사용할 계정을 입력한다.</li>
<li>그 후 비밀번호를 두 번 입력하면 설치가 완료된다.</li>
</ol>
<h2 id="장단점"><a href="#장단점" class="headerlink" title="장단점"></a>장단점</h2><p>시스템 구축과 관련된 일을 하다보면 윈도우에서는 제약이 너무 많아서 리눅스 환경이 가장 적합하다고 느껴진다.<br>개발 일을 해도 Python이나 Node.js 등등 윈도우를 지원하긴 하지만 환경 구성에는 리눅스에 비빌 수가 없다.</p>
<p>그런데 막상 우분투 데스크탑을 깔아버리면 뱅킹도 안되고 게임도 안되고 쓰다가 지쳐서 에잉 다신 안 써 하면서 윈도우로 돌아오게 된다.<br>그러고 다시 아 불편해 리눅스 깔까? 무한 반복이다.</p>
<p>그 때 나를 구원해준 것이 바로 WSL이었다.<br>윈도우의 안정성과 보편성을 가진 채로 리눅스의 매력까지 즐길 수 있게 된 것이었다.</p>
<p>성능도 나름 나쁘지 않았고 MobaXterm과 같은 도구를 이용하면 아주 손쉽게 PyCharm 등의 GUI 소프트웨어도 실행해서 사용할 수 있었다.<br>그래서 지금까지도 계속 쓰고 있다.</p>
<p>한 가지 아쉬운 점은 Docker를 못 쓴다는 것이다.<br>찾아보니 파일 시스템 속도도 좀 느리고 리눅스 시스템 콜을 제대로 지원하지 못한다는 단점도 있다고 한다.<br>근데 나는 Docker만 빼면 그냥 그럭저럭 좋은 것 같다.</p>
<p>WSL을 이용해서 지금 어떻게 작업을 하고 있는지<br>설정 방법 등의 내용은 다음에 따로 정리해서 올릴 것이다.</p>
<h2 id="참고자료"><a href="#참고자료" class="headerlink" title="참고자료"></a>참고자료</h2><ul>
<li><a href="https://webdir.tistory.com/541">윈도우즈에서 리눅스 설치 - WSL</a></li>
</ul>
]]></content>
      <categories>
        <category>개발 환경 구축</category>
      </categories>
      <tags>
        <tag>WSL</tag>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>Python Enum 클래스</title>
    <url>/2019/08/10/2019-08-10-python-enum/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2><p>현실의 문제를 프로그래밍으로 다루려고 고민하다 보면 조건에 따른 분기를 어떻게 처리할 것인지에 대해 생각하게 된다.<br>이 때 단순하고 전통적인 방식으로 조건을 확인하고 맞으면 이거, 아니면 저거 식의 if-else문을 사용해도 되지만<br>더 나은 프로그래밍 방식에 대한 가치관을 세우고 객체지향, 함수형 등의 다양한 개념을 접하다 보면<br>사람에 따라, 상황에 따라 열거형 클래스 등을 이용해서 처리하기도 한다.</p>
<p>Python에서는 3.4 버전부터 열거형 클래스인 Enum을 기본으로 탑재하였다.<br>이 문서에서는 이것을 사용하는 방법을 정리한다.</p>
<span id="more"></span>

<h2 id="사용법"><a href="#사용법" class="headerlink" title="사용법"></a>사용법</h2><h3 id="기본"><a href="#기본" class="headerlink" title="기본"></a>기본</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> enum</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Color</span>(<span class="params">enum.Enum</span>):</span></span><br><span class="line">    RED = <span class="number">0</span></span><br><span class="line">    GREEN = <span class="number">1</span></span><br><span class="line">    BLUE = <span class="number">2</span></span><br></pre></td></tr></table></figure>

<p><code>enum.Enum</code> 클래스를 상속받는 <code>Color</code> 클래스는 이제 열거형 상수로 이용된다.<br>이 예제에서는 정수를 이용하나 정수 뿐만 아니라 모든 자료형을 사용할 수 있다.</p>
<p>기본적인 사용에서는 다음과 같은 결과를 가져올 수 있다.<br>각 코드에 대한 반환값은 다음 줄에 주석으로 표기하였다.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(Color)</span><br><span class="line"><span class="comment"># &lt;enum &#x27;Color&#x27;&gt;</span></span><br><span class="line"></span><br><span class="line">print(Color.RED, Color.BLUE, Color.GREEN)</span><br><span class="line"><span class="comment"># Color.RED Color.BLUE Color.GREEN</span></span><br><span class="line"></span><br><span class="line">print(Color.RED.name)</span><br><span class="line"><span class="comment"># RED</span></span><br><span class="line"></span><br><span class="line">print(Color.RED.value)</span><br><span class="line"><span class="comment"># 0</span></span><br><span class="line"></span><br><span class="line">print(<span class="built_in">repr</span>(Color.RED))</span><br><span class="line"><span class="comment"># &lt;Color.RED: 0&gt;</span></span><br><span class="line"></span><br><span class="line">print(<span class="built_in">type</span>(Color.RED))</span><br><span class="line"><span class="comment"># &lt;enum &#x27;Color&#x27;&gt;</span></span><br><span class="line"></span><br><span class="line">print(Color(<span class="number">0</span>), Color(<span class="number">2</span>))</span><br><span class="line"><span class="comment"># Color.RED Color.BLUE</span></span><br><span class="line"></span><br><span class="line">print(Color[<span class="string">&#x27;RED&#x27;</span>], Color[<span class="string">&#x27;BLUE&#x27;</span>])</span><br><span class="line"><span class="comment"># Color.RED Color.BLUE</span></span><br><span class="line"></span><br><span class="line">print(Color.RED == Color.RED)</span><br><span class="line"><span class="comment"># True</span></span><br><span class="line"></span><br><span class="line">print(Color.RED == <span class="number">0</span>)</span><br><span class="line"><span class="comment"># False</span></span><br><span class="line"><span class="comment">## enum을 정수와 비교하는 것은 항상 False</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> c <span class="keyword">in</span> Color:</span><br><span class="line">    print(c)</span><br><span class="line"><span class="comment"># Color.RED</span></span><br><span class="line"><span class="comment"># Color.GREEN</span></span><br><span class="line"><span class="comment"># Color.BLUE</span></span><br><span class="line"></span><br><span class="line">print(<span class="built_in">list</span>(Color))</span><br><span class="line"><span class="comment"># [&lt;Color.RED: 0&gt;, &lt;Color.GREEN: 1&gt;, &lt;Color.BLUE: 2&gt;]</span></span><br></pre></td></tr></table></figure>

<h3 id="Unique"><a href="#Unique" class="headerlink" title="Unique"></a>Unique</h3><p>열거형 데이터의 인덱스에 대한 유일성을 보장해야 하는 경우가 있을 수 있다.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@enum.unique</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foods</span>(<span class="params">enum.Enum</span>):</span></span><br><span class="line">    SNACK = <span class="number">0</span></span><br><span class="line">    COKE = <span class="number">1</span></span><br><span class="line">    CIDER = <span class="number">1</span></span><br><span class="line">    JUICE = <span class="number">1</span></span><br><span class="line">    PIZZA = <span class="number">2</span></span><br><span class="line">    BURGER = <span class="number">2</span></span><br></pre></td></tr></table></figure>

<p><code>enum.unique</code>라는 Decoration을 사용하면 위와 같이 중복된 값을 가지는 속성이 있는 경우 에러를 발생시킨다.</p>
<h3 id="Auto"><a href="#Auto" class="headerlink" title="Auto"></a>Auto</h3><p>Python 3.6 버전 이상에서는 <code>enum.auto()</code> 함수를 이용하여 자동으로 인덱스를 할당할 수 있다.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NewColor</span>(<span class="params">enum.Enum</span>):</span></span><br><span class="line">    RED = enum.auto()</span><br><span class="line">    GREEN = enum.auto()</span><br><span class="line">    BLUE = enum.auto()</span><br></pre></td></tr></table></figure>

<p>인덱스는 1부터 시작하고 1씩 증가한다.</p>
<h2 id="참고자료"><a href="#참고자료" class="headerlink" title="참고자료"></a>참고자료</h2><ul>
<li><a href="https://pythonkim.tistory.com/90">enum 클래스</a></li>
</ul>
]]></content>
      <categories>
        <category>프로그래밍</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Enum</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu에 Zsh 구성하기</title>
    <url>/2019/08/10/2019-08-11-install-zsh-on-ubuntu/</url>
    <content><![CDATA[<h2 id="개요"><a href="#개요" class="headerlink" title="개요"></a>개요</h2>

<p>개발 일을 하는 사람에게 뗄래야 뗄 수 없는 것이 바로 터미널 소프트웨어이다.<br>이왕 계속 보는 거 좀 이쁘면 기분이 좋지 않을까?</p>
<p>나와 같은 생각을 했던 많은 개발자들이 탄탄대로를 만들어 놓았다.<br>이 문서에서는 Ubuntu 환경에 Zsh를 설치하고 구성하는 방법을 정리한다.</p>
<span id="more"></span>

<h2 id="Zsh-설치"><a href="#Zsh-설치" class="headerlink" title="Zsh 설치"></a>Zsh 설치</h2><p>다음 명령어를 이용하여 설치할 수 있다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install zsh</span><br></pre></td></tr></table></figure>

<p>다음 명령어를 이용하여 zsh를 현재 계정의 기본 쉘로 변경할 수 있다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chsh -s /usr/bin/zsh</span><br></pre></td></tr></table></figure>

<h2 id="oh-my-zsh-구성"><a href="#oh-my-zsh-구성" class="headerlink" title="oh-my-zsh 구성"></a>oh-my-zsh 구성</h2><h3 id="oh-my-zsh-설치"><a href="#oh-my-zsh-설치" class="headerlink" title="oh-my-zsh 설치"></a>oh-my-zsh 설치</h3><p>다음 명령어를 이용하여 설치할 수 있다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh -c <span class="string">&quot;<span class="subst">$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)</span>&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="테마-변경"><a href="#테마-변경" class="headerlink" title="테마 변경"></a>테마 변경</h3><p>다음과 같이 <code>~/.zshrc</code> 파일을 수정하여 <code>agnoster</code> 테마를 사용하도록 설정한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">THEME=<span class="string">&quot;agnoster&quot;</span></span><br></pre></td></tr></table></figure>

<p>다음 명령어를 이용하여 변경사항을 반영한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.zshrc</span><br></pre></td></tr></table></figure>

<p>폰트가 깨질 경우 Powerline을 지원하는 폰트를 설치한다.</p>
<p>필자는 D2Coding 폰트를 설치하였다.</p>
<p>폰트 설치 방법은 추후 별도의 포스트로 정리하도록 하겠다.</p>
<h3 id="Agnoster-테마-컴퓨터-이름-숨기기-설정"><a href="#Agnoster-테마-컴퓨터-이름-숨기기-설정" class="headerlink" title="Agnoster 테마 컴퓨터 이름 숨기기 설정"></a>Agnoster 테마 컴퓨터 이름 숨기기 설정</h3><p>Agnoster 테마는 기본적으로 매번 입력을 받을 때마다 계정 이름과 컴퓨터 이름을 표시한다.</p>
<p>이 때문에 조금만 긴 명령어를 입력하고자 하면 쉽게 라인을 넘어가는 경우가 생긴다.</p>
<p>조금 불편하기 때문에 컴퓨터 이름을 표시하지 않게 설정하여 이런 경우를 줄일 수 있다.</p>
<p>다음 구문을 <code>~/.zshrc</code> 파일에 추가한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">prompt_context</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [[ <span class="string">&quot;<span class="variable">$USER</span>&quot;</span> != <span class="string">&quot;<span class="variable">$DEFAULT_USER</span>&quot;</span> || -n <span class="string">&quot;<span class="variable">$SSH_CLIENT</span>&quot;</span> ]]; <span class="keyword">then</span></span><br><span class="line">    prompt_segment black default <span class="string">&quot;%(!.%&#123;%F&#123;yellow&#125;%&#125;.)<span class="variable">$USER</span>&quot;</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="New-Line-설정"><a href="#New-Line-설정" class="headerlink" title="New Line 설정"></a>New Line 설정</h3><p>Agnoster 테마는 입력 라인에 표시하는 정보가 많아 조금만 긴 명령어를 입력하면 쉽게 라인을 넘어간다.</p>
<p>입력을 다음 라인부터 받게 하여 이러한 경우를 줄일 수 있다.</p>
<p><code>~/.oh-my-zsh/themes/agnoster.zsh-theme</code> 파일을 다음과 같이 수정한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">### Main prompt</span></span><br><span class="line"><span class="function"><span class="title">build_prompt</span></span>() &#123;</span><br><span class="line">  RETVAL=$?</span><br><span class="line">  prompt_status</span><br><span class="line">  prompt_virtualenv</span><br><span class="line">  prompt_aws</span><br><span class="line">  prompt_context</span><br><span class="line">  prompt_dir</span><br><span class="line">  prompt_git</span><br><span class="line">  prompt_bzr</span><br><span class="line">  prompt_hg</span><br><span class="line">  prompt_newline</span><br><span class="line">  prompt_end</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">prompt_newline</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [[ -n <span class="variable">$CURRENT_BG</span> ]]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> -n <span class="string">&quot; %&#123;%k%F&#123;<span class="variable">$CURRENT_BG</span>&#125;%&#125;<span class="variable">$SEGMENT_SEPARATOR</span></span></span><br><span class="line"><span class="string">    %(?.%F&#123;<span class="variable">$CURRENT_BG</span>&#125;.%F&#123;red&#125;)❯%f&quot;</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="built_in">echo</span> -n <span class="string">&quot;%&#123;%k%&#125;&quot;</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> -n <span class="string">&quot;%&#123;%f%&#125;&quot;</span></span><br><span class="line">  CURRENT_BG=<span class="string">&#x27;&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="zsh-autosuggestions-플러그인-설치"><a href="#zsh-autosuggestions-플러그인-설치" class="headerlink" title="zsh-autosuggestions 플러그인 설치"></a>zsh-autosuggestions 플러그인 설치</h3><p><a href="https://github.com/zsh-users/zsh-autosuggestions">zsh-autosuggestions GitHub Repository</a> 페이지를 참고하여 플러그인을 설치한다.</p>
<p>다음과 같이 <code>~/.zshrc</code> 파일을 수정하여 설치한 플러그인을 적용한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">plugins=(</span><br><span class="line">  ...</span><br><span class="line">  zsh-autosuggestions</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>다음 명령어를 이용하여 변경사항을 반영한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.zshrc</span><br></pre></td></tr></table></figure>

<h3 id="zsh-syntax-highlighting-플러그인-설치"><a href="#zsh-syntax-highlighting-플러그인-설치" class="headerlink" title="zsh-syntax-highlighting 플러그인 설치"></a>zsh-syntax-highlighting 플러그인 설치</h3><p><a href="https://github.com/zsh-users/zsh-syntax-highlighting">zsh-syntax-highlighting GitHub Repository</a> 페이지를 참고하여 플러그인을 설치한다.</p>
<p>다음과 같이 <code>~/.zshrc</code> 파일을 수정하여 설치한 플러그인을 적용한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">plugins=(</span><br><span class="line">  ...</span><br><span class="line">  zsh-syntax-highlighting</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>다음 명령어를 이용하여 변경사항을 반영한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.zshrc</span><br></pre></td></tr></table></figure>

<h3 id="autojump-플러그인-설치"><a href="#autojump-플러그인-설치" class="headerlink" title="autojump 플러그인 설치"></a>autojump 플러그인 설치</h3><p>다음 명령어를 이용하여 플러그인을 설치한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install autojump</span><br></pre></td></tr></table></figure>

<p>다음과 같이 <code>~/.zshrc</code> 파일을 수정하여 설치한 플러그인을 적용한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">plugins=(</span><br><span class="line">  ...</span><br><span class="line">  autojump</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>다음 명령어를 이용하여 변경사항을 반영한다.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.zshrc</span><br></pre></td></tr></table></figure>

<h2 id="참고자료"><a href="#참고자료" class="headerlink" title="참고자료"></a>참고자료</h2><ul>
<li><a href="https://www.flaticon.com/authors/smashicons">Icons made by Smashicons CC 3.0 BY</a></li>
<li><a href="https://the-illusionist.me/47">우분투에 zsh와 oh my zsh 설치하기</a></li>
<li><a href="https://medium.com/harrythegreat/oh-my-zsh-iterm2%EB%A1%9C-%ED%84%B0%EB%AF%B8%EB%84%90%EC%9D%84-%EB%8D%94-%EA%B0%95%EB%A0%A5%ED%95%98%EA%B2%8C-a105f2c01bec">Oh My ZSH+ iTerm2로 터미널을 더 강력하게</a></li>
</ul>
]]></content>
      <categories>
        <category>개발 환경 구축</category>
      </categories>
      <tags>
        <tag>Terminal</tag>
        <tag>Zsh</tag>
      </tags>
  </entry>
</search>
